import React, { FC } from 'react';

import './Combat.scss';

export const Combat: FC = () => {

    return (
        <div className='container'>
            <h1>Combat Simulator</h1>
        </div>
    );
};
